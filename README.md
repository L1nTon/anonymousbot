# Anonymous Bot 🤖

Telegram-бот для отправки анонимных сообщений администраторам с возможностью ответа.

## Возможности

- 📨 Отправка анонимных сообщений
- 💬 Администратор может отвечать на сообщения
- ✅ Подтверждение перед отправкой
- 🔐 Полная анонимность (пользователи не видят друг друга)
- 📝 Поддержка длинных сообщений (до 4096 символов)
- 💾 Сохранение истории сообщений в JSON базе данных

## Требования

- Python 3.9+
- Telegram Bot API token
- ID администратора для получения сообщений

## Установка

1. **Клонируем репозиторий**
   ```bash
   git clone <repository-url>
   cd anonymousbot
   ```

2. **Создаем виртуальное окружение** (рекомендуется)
   ```bash
   ./venv_setup.sh create
   source venv/bin/activate
   ```

3. **Устанавливаем зависимости**
   ```bash
   ./venv_setup.sh install
   # или
   pip install -r requirements.txt
   ```

4. **Создаем файл .env**
   ```bash
   cp .env.example .env
   ```

5. **Заполняем .env файл**
   ```
   TELEGRAM_BOT_TOKEN=your_bot_token_here
   ADMIN_ID=your_admin_id_here
   ```

   Где:
   - `TELEGRAM_BOT_TOKEN` - токен вашего бота от @BotFather
   - `ADMIN_ID` - ваш Telegram ID (узнать можно у @userinfobot)

## Использование

### Для обычных пользователей

1. **Запускаем бота**
   ```bash
   python bot.py
   ```

2. **В Telegram**
   - Найдите своего бота и напишите `/start`
   - Используйте команду `/send` для отправки анонимного сообщения
   - Подтвердите отправку
   - Получите ответ администратора

### Для администратора

1. **Получение сообщений**
   - Администратор получает каждое новое сообщение с уникальным ID
   - Под каждым сообщением есть кнопка "💬 Ответить"

2. **Отправка ответа**
   - Нажмите кнопку "💬 Ответить"
   - Напишите свой ответ
   - Ответ отправится пользователю с пометкой "Ответ администратора"

## Команды бота

### Для всех пользователей
- `/start` - Начало работы
- `/send` - Отправить анонимное сообщение
- `/help` - Справка по командам
- `/cancel` - Отменить текущую операцию
- `/myid` - Узнать ваш Telegram ID

### Только для администратора
- `/messages` - Список всех сообщений в базе данных
- Кнопка "💬 Ответить" под каждым анонимным сообщением

## Получение необходимых данных

### Telegram Bot Token
1. Напишите @BotFather в Telegram
2. Выберите или создайте бота
3. Скопируйте токен

### ADMIN_ID
1. Напишите @userinfobot в Telegram
2. Бот покажет ваш ID

## Структура проекта

```
anonymousbot/
├── bot.py                      # Основной файл бота
├── requirements.txt            # Зависимости проекта
├── .env.example               # Пример файла конфигурации
├── .gitignore                 # Игнорируемые файлы
├── venv_setup.sh              # Помощник для управления venv (Linux/Mac)
├── venv_helper.py             # Помощник для управления venv (кроссплатформенно)
├── messages_database.json     # База данных сообщений (создается автоматически)
└── README.md                  # Этот файл
```

## Помощники для управления окружением

### Bash скрипт (Linux/MacOS)

```bash
# Создать виртуальное окружение
./venv_setup.sh create

# Активировать окружение
source venv/bin/activate

# Установить зависимости
./venv_setup.sh install

# Посмотреть статус
./venv_setup.sh status

# Получить справку
./venv_setup.sh help
```

### Python помощник (кроссплатформенно)

```bash
# Создать виртуальное окружение
python venv_helper.py create

# Установить зависимости
python venv_helper.py install-req

# Установить отдельный пакет
python venv_helper.py install requests

# Посмотреть статус
python venv_helper.py status
```

## Запуск в фоне (Linux/Mac)

```bash
nohup python bot.py > bot.log 2>&1 &
```

## Запуск как systemd сервис (Linux)

Создайте файл `/etc/systemd/system/anonymousbot.service`:

```ini
[Unit]
Description=Anonymous Telegram Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/anonymousbot
ExecStart=/usr/bin/python3 /path/to/anonymousbot/bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl start anonymousbot
sudo systemctl enable anonymousbot
```

## Безопасность

⚠️ **Важно:**
- Никогда не коммитьте файл `.env` в репозиторий
- Защищайте ваш токен и ID администратора
- Добавьте `.env` в `.gitignore` (уже добавлено по умолчанию)
- База данных `messages_database.json` также автоматически игнорируется

## Лицензия

MIT

## Автор

Anonymous Bot Project
