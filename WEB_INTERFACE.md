# 🌐 Веб-интерфейс для Anonymous Bot

## Описание

Веб-интерфейс предоставляет удобную панель управления для просмотра и ответа на анонимные сообщения через браузер.

## Возможности

✅ **Просмотр всех чатов** - список всех пользователей, отправивших сообщения
✅ **История сообщений** - полная история переписки с каждым пользователем
✅ **Отправка ответов** - быстрая отправка ответов прямо из браузера
✅ **Статистика** - общая статистика по сообщениям
✅ **Автообновление** - автоматическое обновление каждые 10 секунд
✅ **Уведомления** - визуальные уведомления о непрочитанных сообщениях
✅ **Адаптивный дизайн** - красивый современный интерфейс

## Установка

Зависимости уже добавлены в `requirements.txt`:

```bash
source venv/bin/activate
pip install -r requirements.txt
```

## Запуск

### Вариант 1: Только веб-интерфейс

```bash
source venv/bin/activate
python web_app.py
```

Откройте в браузере: **http://localhost:5000**

### Вариант 2: Бот + Веб-интерфейс одновременно

В одном терминале:
```bash
source venv/bin/activate
python bot.py
```

В другом терминале:
```bash
source venv/bin/activate
python web_app.py
```

## Использование

### 1. Главная страница

При открытии вы увидите:
- **Заголовок** с названием бота
- **Статистика** в верхней части:
  - Всего сообщений
  - Без ответа (выделено желтым)
  - Количество уникальных пользователей
- **Список чатов** слева
- **Область сообщений** справа

### 2. Список чатов

Слева отображается список **всех пользователей**, которые нажали /start в боте:
- **Имя пользователя** (или User ID, если имя недоступно)
- **Превью последнего сообщения** (или "👋 Нажал /start", если сообщений нет)
- **Время последнего сообщения** или последней активности
- **Красный бейдж** с количеством неотвеченных сообщений

**Важно:** Теперь в списке отображаются ВСЕ пользователи, даже те, кто еще не отправил ни одного анонимного сообщения, но нажал /start.

Нажмите на чат, чтобы открыть переписку.

### 3. Просмотр сообщений

При выборе чата:
- **Сообщения пользователя** отображаются слева (фиолетовый градиент)
- **Ответы администратора** отображаются справа (голубой фон)
- Каждое сообщение содержит:
  - Текст сообщения
  - Время отправки
  - ID сообщения (для пользовательских сообщений)

### 4. Отправка нового сообщения пользователю

В заголовке чата есть кнопка **"✉️ Написать сообщение"**:
1. Нажмите на кнопку
2. Откроется форма для нового сообщения (желтый фон)
3. Введите текст сообщения
4. Нажмите **"📤 Отправить"**
5. Сообщение будет отправлено пользователю в Telegram с пометкой "Сообщение от администратора"
6. Для отмены нажмите **"Отмена"** или **✕**

### 5. Отправка ответа на анонимное сообщение

Внизу находится форма для ответа:
1. Введите текст ответа в поле
2. Нажмите кнопку **"📤 Отправить"** или **Enter**
3. Ответ будет отправлен пользователю в Telegram
4. Сообщения автоматически обновятся

### 6. Автообновление

- Список чатов обновляется каждые 10 секунд
- Сообщения в открытом чате также обновляются автоматически
- Можно вручную обновить список чатов кнопкой **🔄**

## API Endpoints

Веб-интерфейс предоставляет REST API:

### GET /api/chats
Получить список всех чатов (пользователей)

**Ответ:**
```json
[
  {
    "user_id": 123456789,
    "user_info": {
      "user_id": 123456789,
      "username": "user",
      "first_name": "Имя",
      "full_name": "Имя Фамилия"
    },
    "messages": [...],
    "unread_count": 2,
    "last_message_time": "2025-12-16 01:39:27.208625"
  }
]
```

### GET /api/messages/<user_id>
Получить все сообщения от конкретного пользователя

**Ответ:**
```json
[
  {
    "message_id": "abc123",
    "text": "Текст сообщения",
    "timestamp": "2025-12-16 01:39:24.325680",
    "admin_reply": "Ответ администратора",
    "admin_reply_timestamp": "2025-12-16 01:39:27.208625",
    "user_info": {...}
  }
]
```

### POST /api/send_reply
Отправить ответ на анонимное сообщение пользователю

**Запрос:**
```json
{
  "message_id": "abc123",
  "reply_text": "Текст ответа"
}
```

**Ответ:**
```json
{
  "success": true,
  "message": "Ответ отправлен!"
}
```

### POST /api/send_message
Отправить новое сообщение пользователю от имени администратора

**Запрос:**
```json
{
  "user_id": 123456789,
  "message_text": "Текст сообщения"
}
```

**Ответ:**
```json
{
  "success": true,
  "message": "Сообщение отправлено!"
}
```

### GET /api/stats
Получить статистику

**Ответ:**
```json
{
  "total_messages": 10,
  "answered_messages": 5,
  "unanswered_messages": 5,
  "unique_users": 3
}
```

## Горячие клавиши

- **Enter** - отправить ответ (в поле ввода)
- **Shift + Enter** - новая строка (в поле ввода)

## Технологии

- **Backend**: Flask 3.0.0
- **Frontend**: Vanilla JavaScript (без фреймворков)
- **Стили**: Custom CSS с градиентами
- **API**: RESTful API
- **Telegram**: python-telegram-bot

## Безопасность

⚠️ **Важно:**
- Веб-интерфейс запускается на `0.0.0.0:5000` (доступен из локальной сети)
- Для продакшена используйте:
  - HTTPS
  - Аутентификацию (добавьте логин/пароль)
  - Firewall для ограничения доступа
  - Production WSGI сервер (gunicorn, uWSGI)

## Продакшен

Для запуска в продакшене используйте gunicorn:

```bash
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:5000 web_app:app
```

## Скриншоты функций

### Главная страница
- Современный дизайн с градиентами
- Статистика в реальном времени
- Список чатов с бейджами

### Просмотр сообщений
- Удобное отображение переписки
- Разные цвета для пользователя и администратора
- Временные метки

### Отправка ответов
- Простая форма ввода
- Мгновенная отправка
- Визуальные уведомления

## Поддержка

Если возникли проблемы:
1. Проверьте, что бот запущен и работает
2. Проверьте `.env` файл (TELEGRAM_BOT_TOKEN и ADMIN_ID)
3. Проверьте логи в терминале
4. Убедитесь, что порт 5000 не занят

## Лицензия

MIT

