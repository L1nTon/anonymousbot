# 🔴 Система отправки ошибок и предупреждений

## Дата: 2025-12-17

## 📋 Описание

Реализована автоматическая система отправки всех ошибок (ERROR) и предупреждений (WARNING) администратору по Telegram.

## 🎯 Возможности

### 1. **Автоматическая отправка логов**
- ⚠️ **WARNING** - все предупреждения отправляются администратору
- 🔴 **ERROR** - все ошибки отправляются администратору
- 📊 **Полная информация** - timestamp, имя модуля, уровень, сообщение

### 2. **Обработчик исключений**
- Перехватывает все необработанные исключения в боте
- Отправляет полный traceback администратору
- Уведомляет пользователя об ошибке

### 3. **Тестовая команда**
- `/test_error` - команда для проверки системы (только для администратора)
- Генерирует тестовые WARNING, ERROR и исключение

## 🔧 Технические детали

### Администратор для отчетов

```python
ERROR_REPORT_ADMIN_ID = 1873601165
```

Все ошибки и предупреждения отправляются на этот ID.

### Компоненты системы

#### 1. **TelegramLogHandler**

Кастомный обработчик логов, который:
- Наследуется от `logging.Handler`
- Перехватывает логи уровня WARNING и выше
- Отправляет их в Telegram асинхронно
- Ограничивает длину сообщения (3800 символов)

```python
class TelegramLogHandler(logging.Handler):
    def __init__(self, admin_id: int):
        super().__init__()
        self.admin_id = admin_id
        self.setLevel(logging.WARNING)  # WARNING и ERROR
```

#### 2. **error_handler**

Глобальный обработчик ошибок для бота:
- Перехватывает все исключения
- Формирует полный traceback
- Отправляет администратору
- Уведомляет пользователя

```python
async def error_handler(update: object, context: ContextTypes.DEFAULT_TYPE) -> None:
    """Обработчик всех ошибок в боте"""
    error = context.error
    tb_string = ''.join(traceback.format_exception(None, error, error.__traceback__))
    await send_error_to_admin(context, error_message, "ERROR")
```

#### 3. **send_error_to_admin**

Вспомогательная функция для отправки сообщений об ошибках:
- Форматирует сообщение с emoji
- Использует HTML parse_mode
- Использует `<code>` для моноширинного текста

## 📝 Формат сообщений

### WARNING

```
⚠️ WARNING

2025-12-17 10:30:45 - __main__ - WARNING - Не удалось отправить получателям: [-1001234567890]
```

### ERROR

```
🔴 ERROR

2025-12-17 10:31:20 - __main__ - ERROR - Ошибка при отправке анонимного сообщения: Connection timeout
```

### Исключение (Exception)

```
🔴 ERROR

Exception: ValueError: Invalid user ID

Traceback (most recent call last):
  File "/home/user/bot.py", line 123, in handle_message
    user_id = int(user_data)
ValueError: Invalid user ID
```

## 🧪 Тестирование

### Команда /test_error

Доступна только администратору (ID: 1873601165).

**Что делает:**
1. Отправляет тестовое WARNING
2. Отправляет тестовый ERROR
3. Генерирует тестовое исключение
4. Проверяет, что все сообщения доставлены

**Использование:**
```
/test_error
```

**Ожидаемый результат:**
Вы получите 3 сообщения:
1. ⚠️ WARNING - "Это тестовое предупреждение (WARNING)"
2. 🔴 ERROR - "Это тестовая ошибка (ERROR)"
3. 🔴 ERROR - "Exception: ValueError: Это тестовое исключение..."

## 🚀 Примеры использования

### В коде бота

```python
# Предупреждение
logger.warning(f"⚠️ Не удалось отправить получателям: {failed}")

# Ошибка
logger.error(f"❌ Ошибка при отправке сообщения: {e}")

# Исключение (автоматически перехватывается error_handler)
raise ValueError("Неверный формат данных")
```

### Что вы получите в Telegram

Каждое предупреждение или ошибка автоматически придет вам в Telegram с полной информацией.

## ⚙️ Настройка

### Изменить ID администратора

Отредактируйте в `bot.py`:

```python
ERROR_REPORT_ADMIN_ID = 1873601165  # Ваш ID
```

### Изменить уровень логирования

По умолчанию отправляются WARNING и ERROR. Чтобы отправлять только ERROR:

```python
telegram_handler.setLevel(logging.ERROR)
```

Чтобы отправлять все логи (включая INFO):

```python
telegram_handler.setLevel(logging.INFO)
```

## 🔍 Отладка

### Проверка работы системы

1. Запустите бота
2. Отправьте `/test_error`
3. Проверьте, что получили 3 сообщения об ошибках

### Если сообщения не приходят

1. Проверьте, что `ERROR_REPORT_ADMIN_ID` правильный
2. Проверьте, что бот запущен
3. Проверьте логи в консоли:
   ```
   ✅ Telegram обработчик логов настроен для администратора 1873601165
   ✅ Обработчик ошибок зарегистрирован
   ```

## ✨ Преимущества

- ✅ **Мгновенное оповещение** - узнаете об ошибках сразу
- ✅ **Полная информация** - traceback, timestamp, контекст
- ✅ **Автоматическая работа** - не нужно проверять логи вручную
- ✅ **Удобство** - все ошибки в Telegram
- ✅ **Тестирование** - команда для проверки системы

## 🎉 Готово!

Теперь все ошибки и предупреждения автоматически отправляются вам в Telegram!

