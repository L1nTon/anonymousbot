# 🔄 Миграция на SQLite

## Обзор изменений

Проект был полностью переведен с JSON-файлов на SQLite базу данных для более надежного хранения данных и улучшенной производительности.

## Что изменилось

### ✅ Новая архитектура

**Было (JSON):**
- `messages_database.json` - хранение сообщений
- `users_database.json` - хранение пользователей
- Ручное чтение/запись файлов
- Риск потери данных при одновременной записи

**Стало (SQLite):**
- `anonymous_bot.db` - единая реляционная база данных
- Три таблицы: `users`, `messages`, `admin_replies`
- Автоматическое управление транзакциями
- Поддержка связей между таблицами

### 📊 Структура базы данных

#### Таблица `users`
```sql
CREATE TABLE users (
    user_id INTEGER PRIMARY KEY,
    username TEXT,
    first_name TEXT,
    last_name TEXT,
    full_name TEXT,
    is_bot INTEGER DEFAULT 0,
    is_premium INTEGER DEFAULT 0,
    language_code TEXT,
    first_seen TEXT,
    last_seen TEXT
)
```

#### Таблица `messages`
```sql
CREATE TABLE messages (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    message_id TEXT UNIQUE NOT NULL,
    user_id INTEGER NOT NULL,
    message_text TEXT NOT NULL,
    message_length INTEGER,
    timestamp TEXT NOT NULL,
    admin_message_id INTEGER,
    is_from_admin INTEGER DEFAULT 0,
    FOREIGN KEY (user_id) REFERENCES users(user_id)
)
```

#### Таблица `admin_replies`
```sql
CREATE TABLE admin_replies (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    message_id TEXT NOT NULL,
    admin_id INTEGER NOT NULL,
    reply_text TEXT NOT NULL,
    timestamp TEXT NOT NULL,
    FOREIGN KEY (message_id) REFERENCES messages(message_id)
)
```

## Новые возможности

### 🎯 Преимущества SQLite

1. **Надежность**: Транзакции ACID гарантируют целостность данных
2. **Производительность**: Быстрые запросы с индексами
3. **Масштабируемость**: Поддержка миллионов записей
4. **История**: Полная история всех ответов на каждое сообщение
5. **Связи**: Правильные связи между пользователями, сообщениями и ответами

### 📝 Новый модуль `database.py`

Создан специальный модуль для работы с базой данных:

```python
from database import Database

db = Database()

# Добавить пользователя
db.add_or_update_user(user_id=123, username="john", ...)

# Добавить сообщение
db.add_message(message_id="abc123", user_id=123, message_text="Hello")

# Добавить ответ
db.add_admin_reply(message_id="abc123", admin_id=456, reply_text="Hi!")

# Получить статистику
stats = db.get_stats()
```

## Изменения в коде

### bot.py
- ❌ Удалены функции `load_database()`, `save_database()`
- ✅ Добавлен `from database import Database`
- ✅ Все операции теперь через `db.add_message()`, `db.get_message()` и т.д.

### web_app.py
- ❌ Удалены функции работы с JSON
- ✅ Все API endpoints переписаны для SQLite
- ✅ Поддержка множественных ответов на одно сообщение

## Миграция данных

### Автоматическая миграция

Запустите скрипт миграции:

```bash
python migrate_to_sqlite.py
```

Скрипт выполнит:
1. ✅ Удаление старых JSON файлов
2. ✅ Создание новой SQLite базы данных
3. ✅ Инициализация таблиц
4. ✅ Проверка структуры

### Ручная миграция (если нужно сохранить старые данные)

Если вы хотите перенести старые данные из JSON в SQLite, создайте скрипт:

```python
import json
from database import Database

db = Database()

# Загрузить старые данные
with open('messages_database.json.backup', 'r') as f:
    old_messages = json.load(f)

# Перенести в SQLite
for msg_id, msg_data in old_messages.items():
    # ... код переноса
```

## Запуск системы

### 1. Запуск бота
```bash
python bot.py
```

### 2. Запуск веб-интерфейса
```bash
python web_app.py
```

### 3. Открыть в браузере
```
http://localhost:5000
```

## Обратная совместимость

⚠️ **Важно**: Старые JSON файлы больше не используются. Все данные теперь в SQLite.

Если вам нужны старые данные, они сохранены в:
- `bot.py.backup` - старая версия бота
- `web_app.py.backup` - старая версия веб-интерфейса

## Тестирование

После миграции протестируйте:

1. ✅ Отправка `/start` в боте
2. ✅ Отправка анонимного сообщения через `/send`
3. ✅ Ответ на сообщение через бота
4. ✅ Просмотр чатов в веб-интерфейсе
5. ✅ Отправка ответа через веб-интерфейс
6. ✅ Просмотр статистики

## Резервное копирование

Для резервного копирования базы данных:

```bash
# Создать бэкап
cp anonymous_bot.db anonymous_bot.db.backup

# Восстановить из бэкапа
cp anonymous_bot.db.backup anonymous_bot.db
```

## Поддержка

Если возникли проблемы:
1. Проверьте логи бота
2. Проверьте, что база данных создана: `ls -la anonymous_bot.db`
3. Проверьте структуру: `sqlite3 anonymous_bot.db ".schema"`

---

**Дата миграции**: 2025-12-16  
**Версия**: 2.0 (SQLite)

