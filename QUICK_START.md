# 🚀 Быстрый старт - Anonymous Bot v2.0

## Что нового в версии 2.0

✅ **SQLite база данных** вместо JSON файлов  
✅ **Полная история чатов** с каждым пользователем  
✅ **Множественные ответы** на одно сообщение  
✅ **Веб-интерфейс** для управления сообщениями  
✅ **Чистая база данных** - начинаем с нуля!

## Установка и запуск

### 1. Активируйте виртуальное окружение

```bash
source venv/bin/activate
```

### 2. Запустите бота

```bash
python bot.py
```

Вы должны увидеть:
```
✅ База данных SQLite инициализирована
✅ База данных SQLite готова к работе
🤖 Бот запущен...
```

### 3. Запустите веб-интерфейс (в новом терминале)

```bash
source venv/bin/activate
python web_app.py
```

Откройте в браузере: **http://localhost:5000**

## Использование

### Для пользователей (в Telegram)

1. **Начать работу с ботом:**
   ```
   /start
   ```

2. **Отправить анонимное сообщение:**
   ```
   /send
   ```
   Затем введите текст сообщения

3. **Отменить отправку:**
   ```
   /cancel
   ```

### Для администратора (в Telegram)

1. **Просмотреть последние сообщения:**
   ```
   /messages
   ```

2. **Ответить на сообщение:**
   - Нажмите кнопку "Ответить" под сообщением
   - Введите текст ответа
   - Ответ будет отправлен пользователю анонимно

### Для администратора (в веб-интерфейсе)

1. **Просмотр всех чатов:**
   - Откройте http://localhost:5000
   - Слева отображается список всех пользователей
   - Красный бейдж показывает количество неотвеченных сообщений

2. **Просмотр истории чата:**
   - Кликните на пользователя в списке
   - Справа отобразится полная история сообщений

3. **Ответ на сообщение:**
   - Под каждым сообщением есть кнопка "Ответить"
   - Введите текст ответа и нажмите "Отправить"
   - Ответ мгновенно доставится пользователю в Telegram

4. **Отправка нового сообщения:**
   - Нажмите кнопку "Новое сообщение" в чате
   - Введите текст и отправьте

## Структура базы данных

База данных SQLite хранится в файле: `anonymous_bot.db`

### Таблицы:

- **users** - все пользователи, которые нажали /start
- **messages** - все сообщения (от пользователей и от админа)
- **admin_replies** - все ответы администратора

## Полезные команды

### Просмотр базы данных

```bash
sqlite3 anonymous_bot.db
```

Внутри SQLite:
```sql
-- Показать все таблицы
.tables

-- Показать структуру таблицы
.schema users

-- Показать всех пользователей
SELECT * FROM users;

-- Показать все сообщения
SELECT * FROM messages;

-- Показать статистику
SELECT COUNT(*) as total_users FROM users;
SELECT COUNT(*) as total_messages FROM messages;
```

### Резервное копирование

```bash
# Создать бэкап
cp anonymous_bot.db backups/anonymous_bot_$(date +%Y%m%d_%H%M%S).db

# Восстановить из бэкапа
cp backups/anonymous_bot_20251216_021950.db anonymous_bot.db
```

### Очистка базы данных

```bash
# Удалить базу данных и создать новую
rm anonymous_bot.db
python migrate_to_sqlite.py
```

## Логи

Логи бота выводятся в консоль. Для сохранения в файл:

```bash
python bot.py 2>&1 | tee bot.log
```

## Порты

- **Веб-интерфейс:** http://localhost:5000
- Если порт 5000 занят, приложение автоматически попробует 5001

## Остановка

### Остановить бота
Нажмите `Ctrl+C` в терминале с ботом

### Остановить веб-интерфейс
Нажмите `Ctrl+C` в терминале с веб-приложением

## Troubleshooting

### Бот не запускается

1. Проверьте `.env` файл:
   ```bash
   cat .env
   ```
   Должны быть установлены:
   - `TELEGRAM_BOT_TOKEN`
   - `ADMIN_ID`

2. Проверьте виртуальное окружение:
   ```bash
   which python
   # Должно показать путь в venv/
   ```

### Веб-интерфейс не открывается

1. Проверьте, что веб-сервер запущен:
   ```bash
   curl http://localhost:5000/api/stats
   ```

2. Проверьте логи в терминале

### База данных повреждена

```bash
# Создать новую базу данных
rm anonymous_bot.db
python migrate_to_sqlite.py
```

## Дополнительная документация

- `MIGRATION_SQLITE.md` - Подробности миграции на SQLite
- `WEB_INTERFACE.md` - Документация веб-интерфейса
- `README.md` - Общая информация о проекте

---

**Версия:** 2.0 (SQLite)  
**Дата:** 2025-12-16

